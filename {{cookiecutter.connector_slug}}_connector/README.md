<!--
SPDX-FileCopyrightText: 2026 {{ cookiecutter.author }}

SPDX-License-Identifier: MIT
-->

> [!IMPORTANT]
> The information below was automatically generated by the [InOrbit Connector Cookiecutter](https://github.com/inorbit-ai/inorbit-connector-cookiecutter). The user is responsible for filling in the details, replacing placeholders and removing this notice.
> The contents below serve as a template for a README.md file.
>
> The following is a to-do list for the user to review after generating the project:
> - [ ] Run `uv sync --extra=dev` if the post gen hook didn't already to create a virtual environment, install dependencies and, most importantly, generate a `uv.lock` file that must be added to versin control.
> - [ ] Revise the contents of:
>   - `README.md`
>   - `CONTRIBUTING.md`
>   - `LICENSE`
>   - `pyproject.toml`
>  - [ ] `git add .` and `git commit -m "Initial commit"` to add the generated files to version control.

# InOrbit {{ cookiecutter.connector_target }} Connector

[![Python {{ cookiecutter.python_version }}](https://img.shields.io/badge/python-{{ cookiecutter.python_version }}-blue.svg)](https://www.python.org/downloads/)

{{ cookiecutter.description }}

## Overview

This repository contains the [InOrbit](https://inorbit.ai/) Edge Connector for [{{ cookiecutter.connector_target }}](https://example.com).

This integration requires the Connector to be configured following the instructions below.

## API Documentation

This connector implementation is based on the official publicly available {{ cookiecutter.connector_target }} API specifications available on the {{ cookiecutter.connector_target }} support portal. For detailed API documentation and reference materials, please refer to:

- [REST API Documentation](https://example.com) (requires a free account login)

## Features

- Multi-robot fleet management through a single connector instance
- Real-time robot monitoring (pose, battery, state, velocity, etc.)
- Automatic retry logic with exponential backoff for API calls
- Background polling architecture for efficient data fetching
- Annotation synchronization for waypoint positions between {{ cookiecutter.connector_target }} and InOrbit
- Built on top of the [`inorbit-connector-python`](https://github.com/inorbit-ai/inorbit-connector-python) framework

## Requirements

- Python {{ cookiecutter.python_version }} or later
- InOrbit account [(it's free to sign up!)](https://control.inorbit.ai/)
- Access to a {{ cookiecutter.connector_target }} server with API credentials
- Network connectivity between the connector host and {{ cookiecutter.connector_target }} server

## Setup

1. Create a Python virtual environment in the host machine and install the connector.

```shell
# Using uv (recommended)
uv sync
```

> [!TIP]
> Installing the `colorlog` package is optional. If available, it will be used to colorize the logs.

```shell
uv pip install colorlog
```

2. Configure the Connector:

- Copy `config/fleet.example.yaml` to `config/my_fleet.yaml` and configure your robot fleet. Each robot needs an InOrbit `robot_id` and the corresponding {{ cookiecutter.connector_target }} `fleet_robot_id`.

- Optionally, configure the {{ cookiecutter.connector_target }} connector-specific settings via environment variables. Copy `config/example.env` to `config/.env` and fill in the values. Any `connector_config` fields can be set using the `INORBIT_{{ cookiecutter.connector_slug.upper() }}_` prefix (e.g., `INORBIT_{{ cookiecutter.connector_slug.upper() }}_FLEET_HOST`, `INORBIT_{{ cookiecutter.connector_slug.upper() }}_FLEET_USERNAME`). Environment variables are used as fallbacks when fields are missing from the YAML configuration. See `config/example.env` for reference. The `.env` file will be automatically loaded when the connector is run.

- Set the `INORBIT_API_KEY` environment variable. You can get the API key for your account from InOrbit's [Developer Console](https://developer.inorbit.ai/docs#configuring-environment-variables).

```bash
export INORBIT_API_KEY=your-api-key-here
# Or place the value in the config/.env file
```

## Deployment

Once all dependencies are installed and the configuration is complete, the Connector can be run as a command.

```bash
source config/.env && {{ cookiecutter.project_slug }} -c config/my_fleet.yaml
```

### Docker

The Connector can be run as a containerized application using Docker Compose:

1. Copy `docker/docker-compose.example.yaml` to `docker/docker-compose.yaml`
2. Copy `config/example.env` to `config/.env` and fill in your credentials
3. Update volume paths in `docker-compose.yaml` to point to your configuration files
4. Run: `docker compose -f docker/docker-compose.yaml up -d`

The Docker Compose setup supports environment variable configuration via `config/.env` and allows running multiple connector instances. See `docker/docker-compose.example.yaml` for detailed configuration options.

## Contributing

Any contribution that you make to this repository will be under the MIT license, as dictated by that [license](https://opensource.org/licenses/MIT).

Please refer to the [CONTRIBUTING.md](CONTRIBUTING.md) file for information on how to contribute to this project.

## License

This project is licensed under the MIT License. See [LICENSE](LICENSE) for details.

## Support

- **Documentation**: [InOrbit Developer Docs](https://developer.inorbit.ai/)
- **Issues**: [GitHub Issues](https://github.com/{{ cookiecutter.github_author }}/{{ cookiecutter.project_slug }}/issues)
- **Email**: {{ cookiecutter.email }}

---

**Powered by InOrbit** | [www.inorbit.ai](https://www.inorbit.ai)
